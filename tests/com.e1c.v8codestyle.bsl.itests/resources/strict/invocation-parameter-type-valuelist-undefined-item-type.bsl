// @strict-types

Procedure Complaint() Export
	
	Number = 1;
	List = New ValueList();
	List.Add(Number);
	
EndProcedure


Procedure Complaint2() Export
	
	Text = "";
    List = New ValueList();
    List.Add(Text);
	
EndProcedure

